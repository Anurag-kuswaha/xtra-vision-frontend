import { createStyles, rem } from '@mantine/core';

const useStyles = createStyles((theme) => ({
    modalWrapper:{
      
       '.mantine-Paper-root':{
        minHeight:'80vh',
        maxWidth:'85vw',
        textAlign:'center'
       }
    },

}));
export default useStyles;